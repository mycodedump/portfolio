Static downloads served as-is at `/portfolio/downloads/<filename>`.

- **job-hunt-tracker.zip** — packaged Chrome extension for the "Job hunt tracker" AI project card. Drop the zipped extension folder here with this exact filename and the download button in the site will pick it up automatically (see `src/app/data/aiProjects.ts`).
